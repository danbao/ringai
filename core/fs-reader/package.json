{
  "name": "@testring/fs-reader",
  "version": "0.8.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./src/index.ts",
  "repository": {
    "type": "git",
    "url": "https://github.com/ringcentral/testring.git"
  },
  "author": "RingCentral",
  "license": "MIT",
  "scripts": {
    "test:performance": "mocha --performance --reporter spec",
    "build": "tsup --config ../../tsup.config.base.ts && tsc -p tsconfig.build.json --declaration --emitDeclarationOnly",
    "test": "mocha"
  },
  "dependencies": {
    "@testring/logger": "workspace:*",
    "@testring/pluggable-module": "workspace:*",
    "@testring/types": "workspace:*",
    "tinyglobby": "0.2.15",
    "p-limit": "3.1.0"
  }
}
